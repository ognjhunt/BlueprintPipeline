# Core dependencies
google-genai==1.0.0
pillow==12.0.0
numpy==1.26.4

# 3D mesh handling
trimesh==4.4.7
pygltflib==1.15.0

# USD support (optional - for USDZ conversion)
# usd-core  # Heavy dependency, may be pre-installed in base image

# PyTorch and CUDA (base for all 3D backends)
torch==2.7.0
torchvision==0.22.0

# HuggingFace for model downloads
huggingface-hub==0.36.2

# For UltraShape integration (includes Hunyuan for Stage 1)
diffusers==0.33.0
transformers==4.57.3
safetensors==0.7.0

# For UltraShape integration
# Reference: https://github.com/PKU-YuanGroup/UltraShape-1.0
accelerate==1.1.1
einops==0.8.1
omegaconf==2.3.0
pytorch-lightning==1.9.5
timm==1.0.22
torchdiffeq==0.2.5

# Image processing (for background removal)
rembg==2.0.57
onnxruntime==1.18.1
opencv-python-headless==4.11.0.86

# Additional mesh processing
pymeshlab==2022.2.post3

# Logging and utilities
tqdm==4.66.5
pyyaml==6.0.2
