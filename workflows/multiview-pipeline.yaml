main:
  params: [event]
  steps:
    - log_event:
        call: sys.log
        args:
          text: ${event}
          severity: "INFO"
        next: extract

    - extract:
        assign:
          - bucket: ${event.data.bucket}
          - object: ${event.data.name}
          - projectId: ${sys.get_env("GOOGLE_CLOUD_PROJECT_ID")}
          - region: "us-central1"
        next: filter_scaled_layout

    # Only handle scenes/.../layout/scene_layout_scaled.json
    - filter_scaled_layout:
        switch:
          - condition: ${text.match_regex(object, "^scenes/.+/layout/scene_layout_scaled\\.json$")}
            next: derive
        next: skip

    - derive:
        assign:
          - parts: ${text.split(object, "/")}
          - sceneId: ${parts[1]}
          - layoutPrefix: ${"scenes/" + sceneId + "/layout"}
          - assetsPrefix: ${"scenes/" + sceneId + "/assets"}
          - segPrefix: ${"scenes/" + sceneId + "/seg"}
          - segDatasetPrefix: ${"scenes/" + sceneId + "/seg/dataset"}
          - multiviewPrefix: ${"scenes/" + sceneId + "/multiview"}
          - jobName: "multiview-job"
          - multiviewAttempts: 0
        next: run_multiview

    - run_multiview:
        call: googleapis.run.v2.projects.locations.jobs.run
        args:
          name: ${"projects/" + projectId + "/locations/" + region + "/jobs/" + jobName}
          body:
            overrides:
              containerOverrides:
                - env:
                    - name: BUCKET
                      value: ${bucket}
                    - name: SCENE_ID
                      value: ${sceneId}
                    - name: LAYOUT_PREFIX
                      value: ${layoutPrefix}
                    - name: SEG_PREFIX
                      value: ${segPrefix}
                    - name: SEG_DATASET_PREFIX
                      value: ${segDatasetPrefix}
                    - name: MULTIVIEW_PREFIX
                      value: ${multiviewPrefix}
                    - name: ENABLE_GEMINI_VIEWS
                      value: "true"
                    - name: VIEWS_PER_OBJECT
                      value: "1"
        result: mvExec
        next: wait_for_multiview

    - wait_for_multiview:
        try:
          call: googleapis.storage.v1.objects.list
          args:
            bucket: ${bucket}
            prefix: ${multiviewPrefix + "/obj_"}
            maxResults: 1
          result: mvList
        except:
          as: e
          steps:
            - handle_mv_error:
                next: retry_multiview
        next: check_multiview

    - check_multiview:
        switch:
          - condition: ${len(mvList.items or []) > 0}
            next: run_assets_plan
        next: retry_multiview

    - retry_multiview:
        switch:
          - condition: ${multiviewAttempts < 60}
            next: bump_multiview_attempt
        next: fail_multiview

    - bump_multiview_attempt:
        assign:
          - multiviewAttempts: ${multiviewAttempts + 1}
        next: sleep_multiview

    - sleep_multiview:
        call: sys.sleep
        args:
          seconds: 10
        next: wait_for_multiview

    - fail_multiview:
        raise: ${"No multiview crops found under " + multiviewPrefix}

    - run_assets_plan:
        call: googleapis.run.v2.projects.locations.jobs.run
        args:
          name: ${"projects/" + projectId + "/locations/" + region + "/jobs/assets-plan-job"}
          body:
            overrides:
              containerOverrides:
                - env:
                    - name: BUCKET
                      value: ${bucket}
                    - name: SCENE_ID
                      value: ${sceneId}
                    - name: LAYOUT_PREFIX
                      value: ${layoutPrefix}
                    - name: SEG_PREFIX
                      value: ${segPrefix}
                    - name: MULTIVIEW_PREFIX
                      value: ${multiviewPrefix}
                    - name: ASSETS_PREFIX
                      value: ${assetsPrefix}
                    - name: SKIP_INTERACTIVE_JOB
                      value: "1"
                    - name: STATIC_ASSET_PIPELINE
                      value: "hunyuan"
        result: assetsPlanExec
        next: wait_for_assets_plan

    - wait_for_assets_plan:
        call: googleapis.run.v2.projects.locations.jobs.executions.get
        args:
          name: ${assetsPlanExec.name}
        result: assetsPlanStatus
        next: check_assets_plan_status

    - check_assets_plan_status:
        switch:
          - condition: ${assetsPlanStatus.completionTime != null}
            next: verify_assets_file
          - condition: ${assetsPlanStatus.failedCount != null and assetsPlanStatus.failedCount > 0}
            next: assets_plan_failed
        next: wait_assets_plan_poll

    - wait_assets_plan_poll:
        call: sys.sleep
        args:
          seconds: 10
        next: wait_for_assets_plan

    - assets_plan_failed:
        raise: ${"Assets-plan job FAILED for scene " + sceneId}

    - verify_assets_file:
        try:
          call: googleapis.storage.v1.objects.get
          args:
            bucket: ${bucket}
            object: ${assetsPrefix + "/scene_assets.json"}
          result: assetsFileCheck
        except:
          as: e
          steps:
            - log_verification_error:
                call: sys.log
                args:
                  text: ${"scene_assets.json not found despite job completion - retrying"}
                  severity: "WARNING"
                next: wait_assets_plan_poll
        next: done

    - done:
        return: ${"assets-plan-job completed successfully for " + object}

    - skip:
        return: ${"multiview skip " + object}