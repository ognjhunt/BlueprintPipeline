main:
  params: [event]
  steps:
    - log_event:
        call: sys.log
        args:
          text: ${event}
          severity: "INFO"
        next: extract

    - extract:
        assign:
          - bucket: ${event.data.bucket}
          - object: ${event.data.name}
          - projectId: ${sys.get_env("GOOGLE_CLOUD_PROJECT_ID")}
          - region: "us-central1"
        next: filter_scaled_layout

    # Only handle scenes/.../layout/scene_layout_scaled.json
    - filter_scaled_layout:
        switch:
          - condition: ${text.match_regex(object, "^scenes/.+/layout/scene_layout_scaled\\.json$")}
            next: derive
          - condition: ${text.match_regex(object, "^scenes/.+/layout/scene_layout_scaled\\.done$")}
            next: derive
        next: skip

    - derive:
        assign:
          - parts: ${text.split(object, "/")}
          - sceneId: ${parts[1]}
          - layoutPrefix: ${"scenes/" + sceneId + "/layout"}
          - assetsPrefix: ${"scenes/" + sceneId + "/assets"}
          - segDatasetPrefix: ${"scenes/" + sceneId + "/seg/dataset"}
          - multiviewPrefix: ${"scenes/" + sceneId + "/multiview"}
          - locksPrefix: ${"scenes/" + sceneId + "/_locks"}
          - doneObject: ${"scenes/" + sceneId + "/layout/scene_layout_scaled.done"}
          - jobName: "multiview-job"
          - multiviewAttempts: 0
        next: gate_multiview_launch

    - gate_multiview_launch:
        switch:
          - condition: ${object == doneObject}
            next: handle_done_marker_event
        next: handle_scaled_event

    - handle_done_marker_event:
        switch:
          - condition: ${event.data.metageneration == "1"}
            next: run_multiview
        next: skip

    - handle_scaled_event:
        try:
          call: googleapis.storage.v1.objects.get
          args:
            bucket: ${bucket}
            object: ${doneObject}
          result: existingDone
          next: skip
        except:
          as: e
          steps:
            - require_metageneration_one:
                switch:
                  - condition: ${event.data.metageneration == "1"}
                    next: run_multiview
                next: skip

    - run_multiview:
        call: run_job_with_lock
        args:
          bucket: ${bucket}
          projectId: ${projectId}
          region: ${region}
          jobName: ${jobName}
          lockObject: ${locksPrefix + "/multiview.json"}
          overrides:
            containerOverrides:
              - env:
                  - name: BUCKET
                    value: ${bucket}
                  - name: SCENE_ID
                    value: ${sceneId}
                  - name: LAYOUT_PREFIX
                    value: ${layoutPrefix}
                  - name: SEG_DATASET_PREFIX
                    value: ${segDatasetPrefix}
                  - name: MULTIVIEW_PREFIX
                    value: ${multiviewPrefix}
                  - name: ENABLE_GEMINI_VIEWS
                    value: "true"
                  - name: VIEWS_PER_OBJECT
                    value: "1"
          dedupeContext:
            eventId: ${event.id}
            eventType: ${event.type}
            metageneration: ${event.data.metageneration}
            dedupeToken: ${locksPrefix + "/multiview.json"}
        result: mvResult
        next: handle_mv_result

    - handle_mv_result:
        switch:
          - condition: ${mvResult.skipped}
            next: done_locked
        next: wait_for_multiview

    - wait_for_multiview:
        try:
          call: googleapis.storage.v1.objects.list
          args:
            bucket: ${bucket}
            prefix: ${multiviewPrefix + "/obj_"}
            maxResults: 1
          result: mvList
        except:
          as: e
          steps:
            - handle_mv_error:
                next: retry_multiview
        next: check_multiview

    - check_multiview:
        switch:
          - condition: ${len(mvList.items or []) > 0}
            next: run_assets_plan
        next: retry_multiview

    - retry_multiview:
        switch:
          - condition: ${multiviewAttempts < 60}
            next: bump_multiview_attempt
        next: fail_multiview

    - bump_multiview_attempt:
        assign:
          - multiviewAttempts: ${multiviewAttempts + 1}
        next: sleep_multiview

    - sleep_multiview:
        call: sys.sleep
        args:
          seconds: 10
        next: wait_for_multiview

    - fail_multiview:
        raise: ${"No multiview crops found under " + multiviewPrefix}

    - run_assets_plan:
        call: run_job_with_lock
        args:
          bucket: ${bucket}
          projectId: ${projectId}
          region: ${region}
          jobName: "assets-plan-job"
          lockObject: ${locksPrefix + "/assets-plan.json"}
        overrides:
          containerOverrides:
            - env:
                - name: BUCKET
                  value: ${bucket}
                - name: SCENE_ID
                  value: ${sceneId}
                - name: LAYOUT_PREFIX
                  value: ${layoutPrefix}
                - name: MULTIVIEW_PREFIX
                  value: ${multiviewPrefix}
                - name: ASSETS_PREFIX
                  value: ${assetsPrefix}
        dedupeContext:
          eventId: ${event.id}
          eventType: ${event.type}
          metageneration: ${event.data.metageneration}
          dedupeToken: ${locksPrefix + "/assets-plan.json"}
        result: assetsPlanResult
        next: handle_assets_plan_result

    - handle_assets_plan_result:
        switch:
          - condition: ${assetsPlanResult.skipped}
            next: done_locked
        next: done

    - done:
        return: ${"assets-plan-job started for " + object}

    - done_locked:
        return: ${"multiview pipeline locked for " + object}

    - skip:
        return: ${"multiview skip " + object}

subworkflows:
  run_job_with_lock:
    params: [bucket, lockObject, projectId, region, jobName, overrides, dedupeContext, maxAttempts]
    steps:
      - init:
          assign:
            - attempts: 0
            - resolvedMaxAttempts: ${maxAttempts or 60}
            - resolvedDedupeContext:
                eventId: ${(dedupeContext and dedupeContext.eventId) or ""}
                eventType: ${(dedupeContext and dedupeContext.eventType) or ""}
                metageneration: ${(dedupeContext and dedupeContext.metageneration) or ""}
                dedupeToken: ${(dedupeContext and dedupeContext.dedupeToken) or lockObject}

      - log_dedupe_context:
          call: sys.log
          args:
            text: '${"Dedupe context for " + jobName + ":eventId=" + resolvedDedupeContext.eventId + ", eventType=" + resolvedDedupeContext.eventType + ", metageneration=" + resolvedDedupeContext.metageneration + ", dedupeToken=" + resolvedDedupeContext.dedupeToken}'
            severity: "INFO"

      - check_lock:
          try:
            call: googleapis.storage.v1.objects.get
            args:
              bucket: ${bucket}
              object: ${lockObject}
            result: existingLock
            next: locked
          except:
            as: e
            steps:
              - create_lock:
                  call: googleapis.storage.v1.objects.insert
                  args:
                    bucket: ${bucket}
                    name: ${lockObject}
                    media:
                      string: ${text.encode_to_base64(text.stringify({"createdAt":sys.now(),"job":jobName}))}
                      contentType: "application/json"

      - log_job_launch:
          call: sys.log
          args:
            text: ${"Launching job " + jobName + " with dedupeToken=" + resolvedDedupeContext.dedupeToken}
            severity: "INFO"

      - start_job:
          call: googleapis.run.v2.projects.locations.jobs.run
          args:
            name: ${"projects/" + projectId + "/locations/" + region + "/jobs/" + jobName}
            body:
              overrides: ${overrides}
          result: execution

      - poll_execution:
          call: googleapis.run.v2.projects.locations.executions.get
          args:
            name: ${execution.name}
          result: execStatus
          next: check_state

      - check_state:
          switch:
            - condition: ${execStatus.state in ["SUCCEEDED", "FAILED", "CANCELLED"]}
              next: clear_lock
            - condition: ${attempts >= resolvedMaxAttempts}
              next: clear_lock
          next: bump_attempts

      - bump_attempts:
          assign:
            - attempts: ${attempts + 1}
          next: sleep_execution

      - sleep_execution:
          call: sys.sleep
          args:
            seconds: 10
          next: poll_execution

      - clear_lock:
          try:
            call: googleapis.storage.v1.objects.delete
            args:
              bucket: ${bucket}
              object: ${lockObject}
          except:
            as: deleteErr
            steps:
              - log_delete_err:
                  call: sys.log
                  args:
                    text: ${"Failed to delete lock " + lockObject + ":" + deleteErr.message}
                    severity: "WARNING"
          next: return_result

      - return_result:
          return:
            skipped: false
            execution: ${execution}
            finalState: ${execStatus.state}

      - locked:
          steps:
            - log_duplicate_skip:
                call: sys.log
                args:
                  text: ${"Duplicate detected for dedupeToken=" + resolvedDedupeContext.dedupeToken + "; skipping eventId=" + resolvedDedupeContext.eventId}
                  severity: "INFO"
          return:
            skipped: true
