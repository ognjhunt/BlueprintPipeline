# Firebase/GCS defaults
BUCKET=blueprint-8c1ca.appspot.com
FIRESTORE_PROJECT=blueprint-8c1ca
SCENE_ID=ChIJIfs1HWblrIkRV4sxRLX4sYM

# Firebase
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/firebase-service-account.json # Firebase service account JSON
FIREBASE_STORAGE_BUCKET=blueprint-8c1ca.appspot.com # Firebase Storage bucket for uploads

# Cost/Alerting
COST_ALERT_PER_SCENE_USD=25.00 # Alert when a scene exceeds this USD cost
ALERT_WEBHOOK_URL=https://example.com/alerts # Webhook URL for alert delivery

# Quality
MIN_QUALITY_SCORE=0.85 # Minimum acceptable quality score (0-1)

# Pipeline environment
PRODUCTION_MODE=false
PIPELINE_ENV=development

# Genie Sim connection
GENIESIM_HOST=localhost
GENIESIM_PORT=50051

# Job path prefixes
ASSETS_PREFIX=scenes/${SCENE_ID}/assets
USD_PREFIX=scenes/${SCENE_ID}/usd
EPISODES_PREFIX=scenes/${SCENE_ID}/episodes
