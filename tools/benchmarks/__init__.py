"""Benchmark utilities for BlueprintPipeline.

This package intentionally keeps dependencies light so benchmark reports can be
generated inside data-factory environments (RunPod, GCP jobs) without pulling
in full training stacks.
"""

