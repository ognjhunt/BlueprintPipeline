# Core dependencies
numpy==1.26.4
pillow==12.0.0

# GCS access
google-cloud-storage==2.18.2

# HTTP client for DWM inference API
requests==2.32.3

# Video encoding (optional, uses ffmpeg CLI if not installed)
imageio==2.36.0
imageio-ffmpeg==0.4.9

# Mesh handling (optional)
trimesh==4.4.7

# Rendering (optional - falls back to mock renderer if not available)
# pyrender>=0.1.45  # Uncomment for better rendering quality

# MANO hand model (optional - falls back to simple hand mesh)
# See MANO_SETUP.md for installation instructions
# smplx>=0.1.28
# torch>=2.0.0

# Telemetry (OpenTelemetry)
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-gcp-trace==1.8.0
opentelemetry-exporter-otlp==1.29.0
opentelemetry-exporter-otlp-proto-grpc==1.29.0
opentelemetry-propagator-gcp==1.8.0
